# ─── External API ─────────────────────────────────────────────────────────────
# When set, the app fetches real data from this base URL.
# When left blank, mock data from /lib/mock/ is used automatically.
NEXT_PUBLIC_API_URL=

# ─── Legacy (no longer used — kept for reference) ────────────────────────────
# MONGODB_URI=mongodb://root:secret@localhost:27017
# NEXTAUTH_SECRET=yournextsecret
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=admin123
